name: Coverage Upload

on:
  push:
    branches: [main]

jobs:
  store-coverage:
    uses: tnoff/github-workflows/.github/workflows/coverage-store.yml@06f676738efdf3f327af0059b3ce0c6f187e1b5c
    with:
      coverage_source: hathor
      install_command: pip install .[test]
      python_version: 3.14
